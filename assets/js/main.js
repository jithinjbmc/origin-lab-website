
const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.nav');if(menuButton){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');document.body.classList.toggle('nav-open',open);menuButton.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');document.body.classList.remove('nav-open');menuButton.setAttribute('aria-expanded','false')}))}
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
document.querySelectorAll('[data-filter-group]').forEach(group=>{const target=group.dataset.filterGroup;group.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{group.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));button.classList.add('active');const value=button.dataset.filter;document.querySelectorAll(`[data-filter-target="${target}"]`).forEach(item=>{item.classList.toggle('hidden',value!=='All'&&item.dataset.category!==value)})}))});
const BACKEND_URL='https://origin-lab-backend.onrender.com';const contact=document.querySelector('#contact-form');if(contact){contact.addEventListener('submit',async event=>{event.preventDefault();const btn=contact.querySelector('button[type="submit"]');const status=contact.querySelector('.form-status');const data=new FormData(contact);const payload={name:data.get('name'),email:data.get('email'),institution:data.get('institution'),inquiryType:data.get('inquiry_type'),message:data.get('message'),website:data.get('website')};btn.disabled=true;btn.textContent='Sending...';if(status){status.textContent='';status.className='form-status';}try{const res=await fetch(`${BACKEND_URL}/api/contact`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const result=await res.json();if(res.ok){contact.reset();if(status){status.textContent='Message sent. Thank you — you will receive a reply by email.';status.className='form-status success';}}else{throw new Error(result.error||'Something went wrong.');}}catch(err){if(status){status.textContent=err.message||'Could not send message. Please email jithin.j@bharatamatacollege.in directly.';status.className='form-status error';}}finally{btn.disabled=false;btn.textContent='Send message';}})}
